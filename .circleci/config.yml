
version: 2.1
orbs: 
  cypress: cypress-io/cypress@2.0.0
jobs:
  build:
    jobs:
      - cypress/run
    steps:
      - checkout
      - run: 'npm install'
      - run: 'npm run build'
      - run: 'npm run test'

workflows:
  my_workflow:
    jobs:
      - build